{
  "name": "node",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "pm2 start server.js && pm2 logs",
    "watch": "pm2 start server.js --watch && pm2 logs",
    "stop": "pm2 stop server.js",
    "logs": "pm2 logs"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.0.2",
    "express": "^4.17.1",
    "mysql": "^2.18.1",
    "regenerator-runtime": "^0.13.5",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {}
}
